.y-image {
  position: relative;
  display: inline-block;
  vertical-align: top;
}

.y-image-img {
  width: 100%;
  height: 100%;
  vertical-align: top;
}

.y-image-loading {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 60rpx;
  height: 60rpx;
}